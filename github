git config --global user.name "leowendell"
git config --global user.email "leonardowendell0147@gmail.com"


git config --global user.name
git config --global user.email

echo "# site_fs60" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/leowendell/site_fs60.git
git push -u origin main

git add.
git commit -m origin main
git push -u main
// REVERTER O GIT ADD.
git restore --staged nome_arquivo.tipo